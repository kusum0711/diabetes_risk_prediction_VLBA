services:
  # MLflow service temporarily disabled to reduce resource usage. Re-enable
  # by restoring this block if you want tracking server locally.
  # mlflow:
  #   image: ghcr.io/mlflow/mlflow:v2.22.0
  #   container_name: mlflow-server
  #   ports:
  #     - "5000:5000"
  #   volumes:
  #     - ./mlflow_data:/mlflow
  #   command: >
  #     mlflow server
  #     --host 0.0.0.0
  #     --port 5000
  #     --backend-store-uri sqlite:///mlflow/mlflow.db
  #     --default-artifact-root /mlflow/artifacts

  diabetes-app: 
    build: .
    container_name: diabetes-app
    working_dir: /app
    volumes:
      - .:/app
    # MLflow disabled: remove MLFLOW env vars and dependency to avoid contacting mlflow
    # If you want to re-enable MLflow, add MLFLOW_TRACKING_URI and depends_on: - mlflow
    command: >
      bash -c "sleep 5 && python -m src.main"